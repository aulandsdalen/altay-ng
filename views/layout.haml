!!!
%html
	%head
		%meta{:charset => 'utf-8'}
		- unless request.path_info == '/' || request.path_info == '/new'
			%link{:rel => "stylesheet", :href => "/css#{request.path_info}.css"}
		- else
			%link{:rel => "stylesheet", :href => "/css/index.css"}
		%title= $ALTAY_APP_VERSION_FULL
	%body
		= yield